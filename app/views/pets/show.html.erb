<h1>Pet Show </h1>

<% if @pet.main_image.attached? %>
    <%= image_tag @pet.main_image %>
    <% elsif @pet.image %> <%# This is to enable seeded photos to populate. Do not delete %>
        <%= image_tag @pet.image %>
<% end %>

<% if @pet.user == current_user %>
        <% link_to "Edit Pet Profile", edit_pet_path %>
<% end %>
<div>
    <%= label_tag "Name:" %>
    <%= @pet.name %>
</div>
<p>
<div>
    <%= label_tag "Breed:" %>
    <%= @pet.breed %>
</div>
<p>
<div>
    <%= label_tag "Rating:" %>
    <%= @pet.rating %>
</div>
<p>
<div>
    <%= label_tag "Bio:" %>
    <%= @pet.bio %>
</div>
<p>

<p>
<div>
    <%= label_tag "Cost:" %>
    $<%= @pet.cost %> per day
</div>
<p>
<div>
<%= label_tag "Owner:" %>
<%= link_to @pet.user.name, user_path(@pet.user) %>
</div>
<p>
<div>
    <%= label_tag "Available:" %>
    <%= @pet.available %>
</div>
<p>

<% if @pet.available == true && @pet.user_id != current_user.id%>
    <div>
        <%# @pet.available == false %>
        <%= button_to("Rent Me", new_rental_path, method: :get, :class => 'ui secondary button') %>
    </div>
<% else %>
    <% if @pet.user_id == current_user.id %>
    <% else %>
    <div>
        <%= button_to("Rent Me", new_rental_path, method: :get, :class => 'ui secondary button') %>
    </div>
<% end %>
    
    <%# Must change @pet.available to false %>
    <%# Add rented pet to user %>
    <%# Can not rent a dog you own %>
<% end %>