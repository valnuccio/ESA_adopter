<% if @user.id == current_user.id %>

    <h1>My Page </h1> 

    <%= button_tag "Edit Profile" %>
    <%# TODO Allow adding of information Location, img, pets%>

    <h3>Location: <%= @user.location %> </h3>
    <h3>Rating: <%= @user.rating %> </h3>

    <% if @user.img_url == nil %>
        <h3> Im Camera shy ^_^ </h3>
    <% else %>
        <%= image_tag(@user.img_url) %>
    <% end %>

    <p> <%= @user.bio %> </p>

    <h3>Pets:</h3>
    <hr>
    <%= link_to "View Other Users", users_path%>
    
    <ul>
        <% @user.my_pets.each do |pet| %>
            <li><%= link_to pet.name, pet_path(pet) %> </li><br>
        <% end %>
    </ul>
    
<%  else  %>
    
    <h1> <%= @user.name %> </h1> 

    <h3> Location: <%= @user.location %> </h3>
    <%# Rating: <%= @user.rating %> 
    
    <% if @user.img_url == nil %>
        <h3> They are camera shy ^_^ </h3>
    <% else %>
        <%= image_tag(@user.img_url) %>
    <% end %>

    <p> <%= @user.bio %> </p>

    <h3>Pets For Rent:</h3>
    <hr>
    <%= link_to "View Other Users", users_path%>
    <ul>
        <% @user.my_pets.each do |pet| %>
            <li><%= link_to pet.name, pet_path(pet) %> <br>
            Available: <%= pet.available %>
            </li>
        <br>
        <% end %>
    </ul>
<% end %>
